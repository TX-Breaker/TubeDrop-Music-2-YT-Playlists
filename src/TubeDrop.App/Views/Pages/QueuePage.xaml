<UserControl x:Class="TubeDrop.App.Views.Pages.QueuePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:TubeDrop.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:QueueViewModel}"
             d:DesignWidth="820" d:DesignHeight="640">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="{Binding Headline}" FontSize="22" FontWeight="SemiBold"
               Foreground="{DynamicResource TubeDrop.Brush.Text}" Margin="2,0,0,10" />

    <ProgressBar Grid.Row="1" Height="6" Minimum="0" Maximum="100"
                 Value="{Binding ProgressPercent, Mode=OneWay}"
                 Foreground="{DynamicResource TubeDrop.Brush.Accent}"
                 Background="{DynamicResource TubeDrop.Brush.Card2}" Margin="2,0,2,12" />

    <Border Grid.Row="2" Background="{DynamicResource TubeDrop.Brush.Card}"
            BorderBrush="{DynamicResource TubeDrop.Brush.Stroke}" BorderThickness="1"
            CornerRadius="{DynamicResource TubeDrop.Radius.Card}">
      <ListView ItemsSource="{Binding Items}" Margin="8" BorderThickness="0" Background="{x:Null}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <Grid Margin="4,5">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TubeDrop.Brush.Text}" />
                <TextBlock Text="{Binding Subtitle}" FontSize="11"
                           Foreground="{DynamicResource TubeDrop.Brush.Text2}" TextTrimming="CharacterEllipsis" />
              </StackPanel>
              <Border Grid.Column="1" VerticalAlignment="Center" Padding="9,3"
                      CornerRadius="{DynamicResource TubeDrop.Radius.Pill}"
                      Background="{DynamicResource TubeDrop.Brush.Card2}">
                <TextBlock Text="{Binding PhaseText}" FontSize="11"
                           Foreground="{Binding PhaseBrushKey, Converter={StaticResource ResourceKeyToBrushConverter}}" />
              </Border>
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </Border>
  </Grid>
</UserControl>
